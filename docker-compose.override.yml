version: "3.7"

services:
  app:
    container_name: app
    restart: always
    build:
      context: .
      dockerfile: Dockerfile.dev
    env_file: .env.dev
    environment:
      - PORT=${PORT}
      - GOOGLE_BASE_URL=${GOOGLE_BASE_URL}
      - GOOGLE_API_KEY=${GOOGLE_API_KEY}
      - X_API_KEY=${X_API_KEY}
    ports:
      - 4005:4005
